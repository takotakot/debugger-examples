{
    // VSCode用C++デバッグ構成（Clangビルドバイナリ用）
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch all_tests",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/all_tests",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build all_tests"
        }
    ]
}
